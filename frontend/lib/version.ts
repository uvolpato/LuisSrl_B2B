/** Versione dell'applicazione — fonte unica.
 *  Aggiornare qui ad ogni rilascio; idealmente creando anche il tag git
 *  corrispondente (es. `git tag v0.1.0`) cosi' la versione e' tracciata su GitHub. */
export const APP_VERSION = "0.1.0";
